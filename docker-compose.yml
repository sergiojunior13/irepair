services:
    # ---- Banco de Dados ----
    db:
        image: mysql:8.0
        restart: unless-stopped
        environment:
            MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
            MYSQL_DATABASE: ${MYSQL_DATABASE}
        volumes:
            - mysql_data:/var/lib/mysql
        ports:
            - "3306:3306" # expõe para o host — necessário para npm run dev acessar o banco
        healthcheck:
            test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
            interval: 10s
            timeout: 5s
            retries: 5

    # ---- Back-End ----
    api:
        build:
            context: ./api
            dockerfile: Dockerfile
        restart: unless-stopped
        depends_on:
            db:
                condition: service_healthy
        environment:
            DATABASE_URL: mysql://root:${MYSQL_ROOT_PASSWORD}@db:3306/${MYSQL_DATABASE}
            JWT_SECRET: ${JWT_SECRET}
            PORT: 3000
            JWT_EXPIRES_IN: 1h
            FRONTEND_ORIGIN: ${FRONTEND_ORIGIN}
        ports:
            - "3000:3000"
        command: sh -c "npx prisma migrate deploy && node dist/src/index.js"

    # ---- Front-End ----
    client:
        build:
            context: ./client
            dockerfile: Dockerfile
            args:
                - VITE_API_URL=${API_URL}
        restart: unless-stopped
        depends_on:
            - api
        ports:
            - "5173:80"

volumes:
    mysql_data:
